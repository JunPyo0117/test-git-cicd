apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: cicd-demo
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: backend
